# Copie para .env e preencha (mesmos valores do web/.env do painel).
# A anon key é pública por design (protegida pela RLS do Supabase).
PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
PUBLIC_SUPABASE_ANON_KEY=sua-anon-public-key
